spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${POSTGRES_DB:carbon_platform}
    username: ${POSTGRES_USER:carbon}
    password: ${POSTGRES_PASSWORD:carbon}

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
